{
  "name": "@cartagraph/core",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "./pkg/index.js",
  "types": "./pkg/index.d.ts",
  "files": ["pkg"],
  "scripts": {
    "build": "cargo test --quiet && wasm-pack build --target web --out-dir pkg",
    "dev": "wasm-pack build --target web --out-dir pkg --dev",
    "test": "cargo test",
    "test:integration": "bun run test-wasm-integration.js",
    "test:webworker": "bun run test-webworker.js",
    "test:all": "cargo test && bun run test:integration && bun run test:webworker",
    "generate-types": "cargo test --quiet",
    "clean": "cargo clean && rm -rf pkg && rm -rf ../shared/src/types/*.ts"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  },
  "dependencies": {
    "@cartagraph/shared": "workspace:*"
  }
}