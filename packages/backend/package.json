{
  "name": "@cartagraph/backend",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun dist/index.js",
    "test": "bun test",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "@cartagraph/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.3.0",
    "@types/bun": "latest"
  }
}